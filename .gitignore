# Segredos — nunca versionar
.env
.env.*
*.token
GITHUB_TOKEN.txt
.git-credentials
client_secret*.json
google_token.json
*_secret_*.json

# Exceção: template .env-default (sanitizado, sem segredos) É versionado
!docker/.env-default

# Ambientes / venvs
.venv/
venv/
__pycache__/
*.pyc

# Infra local
hermes-webui/
backups/

# Editor / SO
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Banco de dados temporários/regeneráveis (exceto os versionados explicitamente)
# OBS: pendencias.db É versionado (exceção abaixo)
!data/pendencias.db
# OBS: processos.db É versionado (compreensão causal de PGEAs — ago/2026)
!data/processos.db
# OBS: atos.db É versionado (catálogo de atos normativos de Boletins de Serviço — ago/2026)
!data/atos.db

# Índice invertido dos MDs — artefato REGENERÁVEL pelo pesquisar_boletins.py
# (não versionar; recriado na primeira execução se ausente)
data/indices/_indice_md.json
